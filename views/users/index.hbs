{{#if currentUser}}
  <h4>Welcome {{currentUser.email}}</h4>
  <a href="/users/{{currentUser._id}}">My Profile</a>
{{/if}}

{{#each users}}
  <ul>
<!--     <li>{{id}}</li> -->
    <li>{{email}}</li>
<!--     <li>{{password_digest}}</li> -->
    {{/each}}
  </ul>

<a href="/users/{{currentUser._id}}/courses/new" class="btn btn-default">Add a New Course</a>
